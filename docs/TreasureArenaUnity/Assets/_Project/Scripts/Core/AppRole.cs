namespace TreasureArenaMR.Core
{
    /// <summary>
    /// Defines which runtime role the Unity application should run as.
    /// </summary>
    public enum AppRole
    {
        Server,
        Manager,
        PicoClient,
        LocalTest
    }
}
