# TreasureArenaMR 项目进度文档

> 用途：答辩PPT素材 | 更新日期：2026-06-17 | 责任人：李潇涵

---

## 一、项目概述

**TreasureArenaMR** 是基于 **Pico 4** 的 MR 多人联机双队夺宝竞技游戏。玩家分红蓝两队，在同一 MR 物理空间内争夺宝物、提交得分、使用武器攻击对手。HP归零后进入"濒死撤离"状态，需真实走回复活区等待倒计时复活——充分利用 MR 空间特性，避免虚拟传送造成的体验割裂。

- **引擎**：Unity（URP管线）
- **网络框架**：Netick（Client-Server权威模型）
- **VR平台**：Pico 4（PICO XR SDK）
- **运行环境**：PC（Server + 管理端）+ Pico 4（客户端），局域网WiFi

---

## 二、系统架构

```
┌─────────────────────────────────────────────────┐
│              Boot.unity（启动入口）                │
│         地图编辑器入口  |  游戏客户端入口             │
└──────────────┬──────────────────────────────────┘
               │
    ┌──────────┴──────────┐
    ▼                     ▼
┌────────────┐      ┌────────────┐
│ Home.unity │      │ Game.unity │
│ 管理端+Server│      │ Pico客户端  │
└─────┬──────┘      └─────┬──────┘
      │                    │
      │  Netick [Networked] │
      │◄──────────────────►│
      │    :7777 (局域网)   │
      │                    │
      ▼                    ▼
 ┌─────────┐          ┌─────────┐
 │ 地图JSON │          │  HUD   │
 │ 配置文件 │          │  MR交互 │
 └─────────┘          └─────────┘
```

**三个场景职责：**

| 场景 | 运行端 | 功能 |
|------|--------|------|
| `Boot.unity` | PC | 启动选择：地图编辑器 / 游戏客户端 |
| `Home.unity` | PC | 管理端UI + Netick Server（房间管理、队伍分配、开始/结束对局） |
| `Game.unity` | PC + Pico 4 | MR游戏场景：地图加载、HUD显示、玩家交互、网络同步 |

**服务器权威原则**：客户端发送意图，服务器决定结果。所有拾取、提交、攻击、扣血、复活判定均在服务端执行。

---

## 三、已完成模块

### 3.1 项目骨架（✅ 100%）

- Unity工程目录结构建立
- `PlayerState` 枚举（Alive / GhostRetreat / Respawning）
- `RoomState` 枚举（Waiting / Ready / Countdown / Playing / Finished）
- `TeamType`、`TreasureType`、`TreasureState` 等核心枚举
- 三个场景可正常切换运行

### 3.2 地图与配置系统（✅ 95%）

**地图JSON协议（已冻结）**：
- `MapJsonModels`：完整的地图JSON数据模型（map_id, objects, team_bases, treasure_spawn_points, supply_boxes, map_boundary）
- `MapLoader`：从StreamingAssets加载地图JSON，反序列化 + 实例化
- `MapValidator`：校验地图JSON完整性（红蓝基地、宝物点、边界等必填字段）
- `RuntimeMapCatalog`：运行时地图索引，支持从`map_catalog.txt`或StreamingAssets目录扫描
- `MapSceneSpawner`：服务端/客户端地图场景加载器，从RoomManager或NetworkMatchState获取地图ID并实例化

**地图JSON文件**（5个，位于`Assets/_Project/StreamingAssets/Maps/`）：
- `game.json`、`map_editor.json`、`map_template_gameplay.json`、`new_map.json`、`pico_map.json`

**房间配置JSON**：
- `RoomConfig`：包含对局时间、玩家HP、武器伤害/射程/冷却、复活倒计时、宝物分值、刷新间隔等全部可配置参数
- 样例配置3份（`config_samples/`）：room_config、weapon_config、treasure_config

**网络消息协议（已冻结）**：
- 11类消息完整定义：join_room、switch_team、start_match、pickup_treasure、submit_treasure、attack、ghost_retreat、respawn_countdown_started、player_respawned、room_state_update、match_finished
- 18种错误码覆盖所有操作场景

### 3.3 地图编辑器（✅ 100%）

**核心编辑功能**：
- 四种编辑模式：放置（Place）、移动（Move）、旋转（Rotate）、缩放（Scale）
- 网格吸附（Grid Snapping）
- 预览虚影（Placement Ghost）：半透明放置预览
- 笔刷系统（Brush）：~56个装饰性prefab（家具、灯具、画作、墙壁、地板等）+ 基础形状（Cube/Cylinder/Circle）+ 玩法标记（TeamBase红蓝、TreasureSpawnPoint各类型、SupplyBox、Bounds）

**输入适配**：
- Pico VR手柄射线输入（右手扳机/握持/摇杆）
- PC键盘回退（WASD变换、鼠标操作）

**导出系统**：
- `MapSceneJsonBuilder`：扫描场景中`MapExportMarker`组件，构建标准地图JSON
- `MapEditorRuntimeExporter`：运行时导出，写入StreamingAssets
- 导出的JSON通过`MapValidator`校验

**UI系统**：
- 笔刷选择下拉框、状态/模式文本、清除/下一模式/删除/校验/导出按钮
- 四周停靠式面板（Docked Panel / Docked UI Controller）
- `MapEditorValidationPresenter`：校验结果展示

**预置体注册表**：
- `PrefabRegistry` ScriptableObject：`prefab_id` → Prefab 映射表
- `MapPrefabRegistry.asset`：注册了60+个可用预制体
- `MapPrefabRegistryBuilder`：编辑器工具一键重建注册表

### 3.4 网络通信（✅ 85%）

**Netick 网络层**：
- `NetworkManager`：Netick.NetworkSandbox 封装，支持 StartAsServer / StartAsClient / StartAsHost / Shutdown
- `LiteNetLibTransport`：基于LiteNetLib的传输层
- Pico真机→PC Server局域网连接已验证通过（阶段5A验收）
- 玩家连接/断开事件完整（OnPlayerConnected / OnPlayerDisconnected）

**状态同步**：
- `NetworkMatchState`（NetworkBehaviour）：服务器权威的匹配状态同步
  - `[Networked]`属性：RoomState、RedScore、BlueScore、RemainingTime、MapIndex、MapRevision
- `RoomStateSynchronizer`：每0.5秒将RoomManager状态写入NetworkMatchState
- Pico客户端通过Netick自动接收复制数据

**玩家管理**：
- `SandboxNetworkListener`：服务器端处理玩家连接→添加房间→自动分队→玩家生成→NetworkPlayer初始化
- `NetworkPlayer`：网络玩家组件，存储player_id、nickname、team、state、hp
- `NetworkTreasure`：网络宝物组件，存储treasure_id、type、state、score_value

### 3.5 服务器权威逻辑（✅ 80%）

**房间管理（RoomManager）**：
- 房间生命周期：Waiting → Playing → Finished
- 玩家加入/离开、自动分队（红蓝均衡）
- 队伍手动切换
- 比赛计时器：倒计时→自动结束
- **濒死撤离复活机制**：
  - GhostRetreat状态检测→玩家走入复活区→开始倒计时→HP恢复→Alive状态
  - 离开复活区取消倒计时（真实物理移动验证）

**宝物权威（ServerTreasureAuthority）**：
- 拾取判定：玩家Alive、未携带宝物、距离≤2.0m
- 提交判定：玩家Alive、携带宝物、在己方基地3.0m范围内
- 分数计算与记录
- `TryPickupNearest`：自动拾取最近宝物

**战斗权威（ServerCombatAuthority）**：
- 攻击请求处理：射线命中检测（hit半径0.5m）
- 伤害计算与HP扣减
- HP归零→GhostRetreat状态→掉落宝物
- `AttackResult`返回：命中/未命中、目标ID、伤害值

### 3.6 Pico客户端（✅ 75%）

**网络接入**：
- `PicoClientNetickBootstrap`：Pico客户端启动器，配置服务器IP:Port，一键连接
- 状态显示：Running / Connected / Disconnected
- 连接/断开按钮

**HUD系统**：
- `HudView`：VR头显HUD，0.5秒刷新显示
  - 连接状态、房间状态（Waiting/Playing/Finished）
  - 实时比分（Red:Blue）、剩余时间（MM:SS）
  - 地图ID
- `PicoMenuPanelController`：Pico菜单面板
  - HMD菜单按钮（或M键）开关Canvas面板
  - 跟随摄像头、平滑淡入淡出

**地图加载**：
- `MapSceneSpawner`：Pico端从NetworkMatchState读取MapIndex→解析mapId→加载地图JSON→实例化场景

### 3.7 管理端UI（✅ 70%）

`ManagerHomeView`：
- 角色/连接状态显示
- 房间创建按钮
- 地图选择（遍历RuntimeMapCatalog）
- 配置展示：HP、武器伤害、复活时间、对局时长
- 红蓝队玩家列表（`PlayerListItem`模板）
- 开始/结束对局按钮
- 服务器状态日志

`ServerBootstrap`：
- 一键启动Server+Room
- 地图选择/切换
- 对局开始条件校验（玩家数、地图加载、房间状态）
- 手动停止对局

### 3.8 当前阻塞问题

**地图JSON在Pico端无法正常显示**（当前最主要阻塞项）：

- PC端（Home.unity/Editor）地图加载和显示正常
- Pico端（Game.unity）通过Netick接收MapIndex → 解析mapId → MapSceneSpawner加载JSON → 实例化场景的链路中，地图物件无法正确渲染
- 可能原因排查方向：
  - `MapSceneSpawner`在Pico端的`ResolveMapSource()`分支：是否正常从`NetworkMatchState`获取到有效的MapIndex和MapRevision
  - `RuntimeMapCatalog`在Pico Build中的行为：StreamingAssets路径在Android/Pico平台下与PC不同，目录扫描可能失败
  - `MapLoader.LoadFromMapId()`在Pico端的文件路径解析是否正确
  - `PrefabRegistry`引用：Pico Build中`MapSceneSpawner._prefabRegistry`是否正确序列化/赋值
  - NetworkMatchState的`MapConfigured`标志是否在Pico端被正确同步（Netick `[Networked]`属性复制时序）
- 此问题阻塞了Pico端完整游戏流程验证

---

## 四、当前进度总表

| 模块 | 完成度 | 关键产出 |
|------|--------|----------|
| 项目骨架 | ✅ 100% | 工程结构、枚举定义、场景框架 |
| 地图JSON协议 | ✅ 100% | 协议冻结、MapJsonModels、样例地图 |
| 地图加载与校验 | ✅ 95% | MapLoader、MapValidator、MapSceneSpawner |
| 房间配置系统 | ✅ 100% | RoomConfig、3份样例配置 |
| 网络消息协议 | ✅ 100% | 11类消息、18种错误码 |
| 地图编辑器 | ✅ 100% | Pico MR编辑、4种模式、导出JSON、校验通过 |
| Netick网络通信 | ✅ 85% | 局域网联机验证通过、状态同步 |
| 服务器权威逻辑 | ✅ 80% | 房间/宝物/战斗权威，濒死复活 |
| Pico客户端 | ✅ 75% | 网络接入、HUD、地图加载 |
| 管理端UI | ✅ 70% | 房间创建、地图选择、分队、开始/结束 |
| 武器系统 | ⚠️ 40% | Gun/Bullet基础脚本有，待与服务器权威对接 |
| 对局结算 | ⚠️ 30% | RoomManager可触发Finished，UI展示待做 |
| **地图JSON Pico端显示** | ❌ 阻塞 | 地图JSON在Pico端无法正常渲染，当前最主要问题 |

**整体MVP完成度：约 70-75%**

---

## 五、待完成工作

### 短期（冲刺完成MVP闭环）

1. **【最高优先级】修复地图JSON在Pico端无法显示的问题**：排查MapSceneSpawner/RuntimeMapCatalog/MapLoader/PrefabRegistry在Pico Build中的行为
2. **多客户端联调**：多台Pico + PC Editor同时接入，完整多人流程测试
3. **宝物网络同步**：Treasure状态（Spawned/Carried/Dropped/Submitted）的Netick网络实例化与同步
4. **客户端请求发送**：拾取、提交、攻击等操作从Pico端发送到服务端
5. **武器系统对接**：Gun/Bullet与ServerCombatAuthority对接，客户端发送攻击请求→服务器判定
6. **结算面板**：对局结果展示（比分、个人统计）
7. **管理端完善**：RoomCreateController、MatchControlController交互完善

### 中期（增强体验）

8. **物资箱系统**：SupplyBox刷新武器/道具
9. **HUD丰富**：HP条、宝物提示、濒死撤离提示、击杀提示
10. **管理端实时状态面板**：对局中实时显示玩家位置、宝物状态、比分变化
11. **MR视觉效果**：弹孔、命中特效、宝物光效
12. **多地图切换**：加载不同地图JSON进行多地图对局

### 远期（超越MVP）

13. 多种武器系统
14. 历史战绩查询 / 排行榜
15. 地图编辑器多人协同编辑
16. 跨公网联机
17. AI怪物/守卫

---

## 六、关键技术亮点

1. **MR濒死撤离机制**：不使用虚拟传送，玩家真实走回复活区——充分利用MR空间特性，客户端服务端双重距离校验
2. **服务器权威架构**：所有关键判定（拾取、提交、攻击命中、扣血、复活）均在服务端执行，防作弊
3. **Pico MR地图编辑器**：在真实物理空间中通过VR手柄放置/调整场景物件，导出为标准JSON，支持Pico手柄+键盘双输入
4. **地图-游戏解耦**：地图通过JSON文件描述，编辑器和游戏逻辑完全分离，`prefab_id`注册机制支持灵活扩展物件库
5. **Netick网络同步**：利用`[Networked]`属性自动复制服务器状态到客户端，减少手动序列化工作量
6. **地图索引同步**：通过`RuntimeMapCatalog`将map_id编码为整数索引，减少网络传输，客户端根据索引还原地图

---

## 七、团队分工

| 成员 | 职责 |
|------|------|
| 李潇han | 项目负责人：架构设计、核心逻辑、Pico客户端接入、Mock调试、集成验收 |
| 崔国庆 | Netick服务端、服务器权威、房间同步、玩家连接 |
| A | Unity管理端UI：房间创建/配置/地图选择/分队/开始结束 |
| B | Pico客户端HUD/游戏内UI：HP/比分/倒计时/宝物提示/结算 |
| C | 场景搭建/Prefab/美术资源：宝物/武器/基地/复活区/物资箱 |

---

## 八、项目文件结构速查

```
TreasureArenaMR/
├── TreasureArenaUnity/Assets/_Project/
│   ├── Scripts/
│   │   ├── Core/          # AppBootstrap、项目常量
│   │   ├── Shared/        # 枚举定义、RoomConfig、MapJsonModels
│   │   ├── Map/           # MapLoader、MapValidator、MapSceneSpawner、PrefabRegistry、RuntimeMapCatalog
│   │   ├── MapEditor/     # 地图编辑器（RuntimeController、Brush、Exporter、UI）
│   │   ├── Server/        # ServerBootstrap、RoomManager、ServerTreasureAuthority、ServerCombatAuthority
│   │   ├── Client/        # PicoClientNetickBootstrap
│   │   ├── Network/       # NetworkManager、SandboxNetworkListener、NetworkMatchState、NetworkPlayer
│   │   ├── UI/            # ManagerHomeView、HudView、PicoMenuPanelController
│   │   ├── Gun/           # Gun、Bullet（武器基础）
│   │   └── Mock/          # 本地测试驱动
│   ├── Scenes/            # Boot、Home、Game、MapEditor、MapPreview
│   ├── Prefabs/
│   │   ├── MapEditor/Palette/Prefabs/  # ~60个装饰预制体
│   │   └── MapEditor/GameplayMarkers/  # 玩法标记预制体
│   ├── ScriptableObjects/Map/
│   │   └── MapPrefabRegistry.asset     # 预制体ID注册表
│   └── StreamingAssets/Maps/           # 5个地图JSON文件
├── config_samples/        # 3份样例配置JSON
├── map_samples/           # 样例地图JSON
└── docs/                  # 设计文档（project_design, json_protocol等）
```
