# PROJECT_CONTEXT

## 项目当前定位

本项目是一个基于 Pico 4 的 MR 多人联机双队夺宝竞技游戏。

项目采用一个 Unity 工程、多运行角色的架构。PC 端运行 Unity 管理端和 Netick 服务端，Pico 4 作为玩家客户端接入。地图由头盔上的地图编辑器摆放预制体并导出 JSON 文件，管理端加载地图并创建多人房间，服务端根据房间配置运行红蓝队夺宝对局。

当前玩法方向已经收敛为：

```text
多人双队夺宝 + 武器攻击 + HP + 濒死撤离 + 复活区倒计时 + 管理端配置 + 服务器权威
```

注意：本项目不再设计正式单人寻宝模式。LocalTest 只作为开发调试手段，用于在设备不足时模拟多人流程。

---

## 已确认内容

### 1. 端侧结构

```text
地图编辑端：
使用头盔中的地图编辑器摆放预制体、地形模块、出生区、复活区、宝物点、物资箱，并导出 JSON 地图文件。

Unity 管理端：
创建多人房间、选择地图、配置规则、设置 HP、武器伤害、复活倒计时、对局时间、宝物分值，分配红蓝队，控制游戏开始和结束。

Unity / Netick 服务端：
运行服务器权威逻辑，负责房间、玩家、队伍、地图加载、宝物生成、拾取、提交、攻击、HP、濒死撤离、复活、分数和结算。

Pico 4 客户端：
负责 MR 画面显示、头显和手柄输入、玩家交互、动画音效、UI 展示，并向服务器发送操作请求。

数据库：
保存玩家、地图、房间配置、对局结果和玩家统计，不参与实时战斗。
```

### 2. 管理端形式

管理端确定使用 Unity 实现，不做 Web 管理端。

MVP 阶段可以采用：

```text
一个 PC Unity 程序 = Manager + Server
Pico 4 = Client
PC Editor = 模拟客户端 / 调试客户端
```

后续如果时间允许，可以将 Manager 和 Server 拆成两个运行角色。

### 3. Unity 工程形式

项目使用一个 Unity 工程，不拆多个 Unity 工程。

通过 AppRole 区分：

```csharp
public enum AppRole
{
    Server,
    Manager,
    PicoClient,
    LocalTest
}
```

### 4. Pico 4 端职责

Pico 4 只作为客户端，不承担权威逻辑。

客户端可以：

```text
采集头显和手柄输入
显示 MR 场景
播放动画音效
显示 UI
发送移动、拾取、攻击、提交请求
```

客户端不可以：

```text
直接决定拾取成功
直接决定提交得分
直接决定攻击命中
直接修改 HP
直接决定死亡
直接决定复活
```

### 5. 数据库

数据库可以做，但只负责保存配置和结果。

数据库不保存：

```text
实时位置
实时 HP
每一帧状态
每一发子弹
实时房间同步
```

数据库保存：

```text
玩家
地图
房间配置
对局结果
玩家统计
可选的武器配置
可选的宝物配置
```

---

## 当前玩法规则

### 多人双队夺宝模式 TeamTreasure

```text
1. 玩家分为红队和蓝队。
2. 地图中生成普通宝物、稀有宝物，可选最终宝物。
3. 玩家可以拾取一个宝物并携带。
4. 玩家需要把宝物带回己方基地提交。
5. 提交成功后队伍获得对应分数。
6. 玩家可以使用武器攻击敌方玩家。
7. 玩家有 HP，最大 HP 可由管理端配置。
8. 武器伤害、射程、冷却可由管理端配置。
9. HP 归零后进入 GhostRetreat 状态。
10. 若死亡时携带宝物，宝物掉落在当前位置。
11. GhostRetreat 状态下，玩家不能攻击、拾取、提交、触发物资箱，也不能被攻击。
12. 玩家需要真实走回己方复活区。
13. 服务器检测到玩家进入己方复活区后，开始复活倒计时。
14. 倒计时结束后，玩家 HP 恢复并重新进入 Alive 状态。
15. 时间结束后，分数高的一队获胜。
```

---

## MR 濒死撤离机制

本项目是 MR 游戏，不能采用普通游戏中“死亡后直接传送回基地”的方式。

原因：

```text
玩家戴着 Pico 4 头盔，真实身体仍然站在原地。
如果只把虚拟角色传送回基地，会导致真实位置和虚拟位置不一致，体验割裂。
```

因此采用濒死撤离机制：

```text
HP 归零
→ 进入 GhostRetreat 状态
→ 携带宝物掉落
→ 禁止攻击、拾取、提交和触发物资箱
→ 不能被敌人攻击
→ 玩家真实走回己方复活区
→ 进入复活区后开始倒计时
→ 倒计时结束后恢复 HP
→ 状态变回 Alive
```

玩家状态：

```csharp
public enum PlayerState
{
    Alive,
    GhostRetreat,
    Respawning
}
```

---

## 服务器权威边界

服务器负责：

```text
房间创建
房间状态
玩家连接
队伍分配
地图加载
宝物生成
宝物拾取判定
宝物提交判定
攻击命中判定
HP 扣减
GhostRetreat 状态切换
死亡掉宝
复活区检测
复活倒计时
分数计算
胜负结算
对局结果写入数据库
```

客户端负责：

```text
Pico 4 输入采集
MR 画面显示
玩家本地交互表现
动画和音效
HUD 和提示 UI
向服务器发送操作请求
```

原则：

```text
客户端发送意图，服务器决定结果。
```

---

## MVP 范围

MVP 必须完成：

```text
1. 一个 Unity 工程。
2. Boot、Home、Game、MapPreview、MapEditor 场景。
3. Unity 管理端可以创建多人房间。
4. 管理端可以选择地图 JSON。
5. 管理端可以配置 HP、武器伤害、复活倒计时、对局时间和宝物分值。
6. 至少两个客户端可以接入同一房间；设备不足时允许 Pico 4 + PC Editor 模拟客户端。
7. 管理端可以分配红队和蓝队。
8. 服务端可以加载地图并生成宝物。
9. 玩家可以拾取、携带、提交宝物。
10. 玩家可以攻击敌方玩家。
11. 服务端可以权威处理攻击、扣血、HP 归零、濒死撤离、复活和得分。
12. 时间结束后服务端结算红蓝队胜负。
13. 对局结果写入数据库。
14. 管理端显示结果。
15. MapEditor 可以选择 prefab 并放置/移动/旋转/缩放/删除对象。
16. MapEditor 可以放置 TeamBase / TreasureSpawnPoint / SupplyBox / Bounds。
17. MapEditor 可以导出地图 JSON，并通过 MapValidator 校验。
```

MVP 暂不做：

```text
1. Web 管理端。
2. 正式单人寻宝模式。
3. 复杂账号系统。
4. 多种复杂武器。
5. 复杂背包。
6. 复杂弹孔和材质破坏。
7. AI 怪物。
8. 对局运行中的实时地图修改。
9. 真实物理抢夺。
10. 跨公网联机。
11. 复杂排行榜筛选。
```

---

## 成员分工

```text
李潇涵：
项目负责人，负责项目架构、Unity 核心逻辑、多人夺宝规则、Pico 客户端基础接入、Mock 多人调试、集成验收。

崔国庆：
负责 Netick 服务端、服务器权威、房间同步、玩家连接、数据库、对局结果保存。

A：
负责 Unity 管理端 UI，包括房间创建、房间配置、地图选择、分队、开始和结束按钮。

B：
负责 Pico 客户端 HUD 和游戏内 UI，包括 HP、比分、倒计时、宝物提示、濒死撤离提示、结算面板。

C：
负责场景搭建、Prefab、地图组件、美术资源，包括宝物、武器、基地、复活区、物资箱和场景表现。
```

---

## 当前推荐开发顺序

```text
第 1 阶段：项目骨架
李潇涵建立 Unity 工程、目录、场景、AppRole、RoomConfig、PlayerState 和 Mock 多人数据。
崔国庆建立 Netick 启动测试、ServerApp、RoomManager 和数据库 schema。

第 2 阶段：地图 JSON
从地图编辑器导出样例 JSON。
李潇涵完成 MapJsonModels、MapLoader、PrefabRegistry 和 MapValidator。
C 协助制作地图组件 Prefab。

并行阶段：地图编辑器 MVP
完成 MapEditor 场景、prefab/玩法标记摆放、四周停靠式基础 UI、JSON 导出和 MapValidator 校验。

第 3 阶段：多人房间基础
崔国庆完成 Server / Client 连接、玩家加入房间、房间状态同步。
李潇涵完成管理端创建房间和开始对局的基础逻辑。
A 开始制作 Home 管理端 UI。

第 4 阶段：夺宝闭环
李潇涵完成宝物生成、拾取、携带、提交、红蓝队计分和时间结算。
B 开始制作 HUD。
C 开始制作宝物、基地、复活区简单 Prefab。

第 5 阶段：战斗和濒死撤离
李潇涵和崔国庆完成攻击、扣血、GhostRetreat、复活区检测、倒计时复活。
B 完成濒死撤离 UI。
C 完成复活区和武器表现。

第 6 阶段：数据库和展示
崔国庆完成对局结果写入数据库。
A 完成结果显示。
李潇涵负责最终集成和验收。
```

---

## AI / Codex 协作注意事项

后续给 AI 或 Codex 分任务时，必须明确：

```text
当前状态
本阶段目标
允许修改
禁止修改
需要先检查的文件
具体任务
测试要求
完成后输出
```

必须强调：

```text
不要擅自修改协议字段
不要破坏当前可运行流程
不要凭空假设项目结构
不要把未完成内容写成已完成
不确定的地方标注 TODO
固定 UI 优先使用 Canvas / Prefab，可视化搭建
代码只负责绑定引用、刷新文本、控制显隐和处理按钮事件
```

---

## 当前最小目标

先做出一个没有正式素材但能运行的多人版本：

```text
PC 管理端创建多人房间
→ Pico 客户端加入
→ PC Editor 模拟另一个客户端加入
→ 地图 JSON 加载
→ 管理端分配红蓝队
→ 拾取宝物
→ 提交得分
→ 武器攻击
→ HP 归零进入 GhostRetreat
→ 玩家走回复活区
→ 倒计时复活
→ 对局结束
→ 结果写入数据库
```
